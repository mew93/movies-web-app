{% load static %}

<link rel="stylesheet" type="text/css" href="{% static 'tommymovies/style.css' %}" />

<h2>Movie Edit</h2>
{% if movie %}
<div class="fieldWrapper">
<form action="{% url 'tommymovies:edit_submit' movie.id %}" method="post">
{%csrf_token %}
	<table>
	<tr>
		<th>Name</th>
		<th><input type="text" name="name"value= "{{ movie.name }}"></th>
	</tr>
	<tr>
		<th>Year</th>
		<th><input type="text" name="year" value= "{{ movie.year }}"></th>
	</tr>
	<tr>
		<th>Watched?</th>
		<th><input type="text" name="watched" value= "{{ movie.watched }}"></th>
	</tr>
	<tr>
		<th>Date Watched</th>
		<th><input type="text" name="watch_date" value= "{{ movie.watch_date }}"></th>
	</tr>
	<tr>
		<th>Personal Rating</th>
		<th><input type="text" name="p_rating" value= "{{ movie.p_rating }}"></th>
	</tr>
	<tr>
		<th>IMDB Rating</th>
		<th><input type="text" name="imdb_rating" value= "{{ movie.imdb_rating }}"></th>
	</tr>
	<tr>
		<th>RT Rating</th>
		<th><input type="text" name="rt_rating" value= "{{ movie.rt_rating }}"></th>
	</tr>

	{% for categories in movie.categories.all %}
	<tr>
		<th>Genre</th>
		<th><input type="text" name="categories" value= "{{ categories }}"></th>
	</tr>
	{% endfor %}

	</table>
	<a href="{{ request.META.HTTP_REFERER }}">Back</a>
	<input type="submit" style="float: right;" value="Submit Changes"/>
</div>
{% else %}
    <p>Cannot find.</p>
{% endif %}


